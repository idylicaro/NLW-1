https://create-react-app.dev/docs/adding-typescript/
https://pt-br.reactjs.org/docs/hooks-state.html


para usar o state no react tem que importa a import React,{ useState } from 'react';
e criar os hooks 
	//destructuring
ex: const [counter, setCounter] = useState(0); // o use state ele retorna o this.state no [0] e o setState no [1];


o a propriedade exact na parte de roteamento react é muito importante , <Route component={Home} path="/" exact />
basicamente ela fala que so vai pra a home se for exatamente / 

com isso feito , para torna um SPA temos que importar na pagina que for usar um <a> e trocar ele pelo componente do import ex:
import { Link } from 'react-router-dom';
<Link to="/create-point"> 

axios para fazer as requests

função do react para quando puxar informações do backend não ficar atualizando(renderizando seila) todos components ...

* sempre que cria um state que seja um array ou um objeto precisa manualmente informar o tipo da variavel
* sempre que o react faz um map , ele precisa do atributo key no primeiro elemento que seja unico entre os dados
